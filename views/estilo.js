import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#a5dee6',
        padding: 20
    }
});

export default styles;